<%= form_with(model: footcard) do |form| %>
  <% if footcard.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(footcard.errors.count, "error") %> prohibited this footcard from being saved:</h2>

      <ul>
        <% footcard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
